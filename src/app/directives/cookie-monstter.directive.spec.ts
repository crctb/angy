import { CookieMonstterDirective } from './cookie-monstter.directive';

describe('CookieMonstterDirective', () => {
  it('should create an instance', () => {
    const directive = new CookieMonstterDirective();
    expect(directive).toBeTruthy();
  });
});
